export {};

declare global {
  namespace Amity {
    type FeedType = 'community' | 'user';

    type Feed = {
      feedId: string;
      feedType: 'reviewing' | 'published';
      targetType: FeedType;
      targetId: string;
      postCount: number;
    } & Amity.Timestamps;
  }
}
